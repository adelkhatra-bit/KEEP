name: KEEP Battle live invite + TikTok gauge repair

on:
  push:
    paths:
      - 'scripts/patch_keep_battle_live_invite_and_tiktok_gauge.py'
      - '.github/workflows/keep-battle-live-invite-tiktok-gauge-once.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: reconcile/claude-main-20260825
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Sync branch
        run: git pull --ff-only origin reconcile/claude-main-20260825
      - name: Apply Battle live invite and gauge repair
        run: python3 scripts/patch_keep_battle_live_invite_and_tiktok_gauge.py
      - name: Install dependencies
        run: npm ci
      - name: Verify mobile + backend
        run: |
          npm --workspace packages/mobile run type-check
          npm --workspace packages/mobile test -- --runInBand BattleNotificationActions.contract.test.ts KeepBattleMobileGameV3.compact.test.ts
          npm --workspace packages/backend run build
      - name: Commit fix
        run: |
          git config user.name "KEEP Guardian"
          git config user.email "actions@github.com"
          git add packages/mobile/src/components/KeepBattleMobileGameV3.tsx packages/mobile/src/services/pushNotificationService.ts packages/mobile/src/screens/__tests__/BattleNotificationActions.contract.test.ts packages/backend/src/lib/pushNotifications.ts
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "fix(battle): repair live invite and 1v1 score gauge"
          git push origin HEAD:reconcile/claude-main-20260825
