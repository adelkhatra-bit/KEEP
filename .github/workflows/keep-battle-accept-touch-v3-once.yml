name: KEEP Battle accept touch repair

on:
  push:
    paths:
      - 'scripts/patch_keep_battle_accept_touch_v3.py'
      - '.github/workflows/keep-battle-accept-touch-v3-once.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: reconcile/claude-main-20260825
          fetch-depth: 0
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: '22'
      - name: Sync branch
        run: git pull --ff-only origin reconcile/claude-main-20260825
      - name: Apply larger accept/refuse controls
        run: python3 scripts/patch_keep_battle_accept_touch_v3.py
      - name: Install dependencies
        run: npm ci
      - name: Verify Battle mobile
        run: |
          npm --workspace packages/mobile run type-check
          npm --workspace packages/mobile test -- --runInBand KeepBattleMobileGameV3.compact.test.ts BattleNotificationActions.contract.test.ts
      - name: Commit fix
        run: |
          git config user.name "KEEP Guardian"
          git config user.email "actions@github.com"
          git add packages/mobile/src/components/KeepBattleMobileGameV3.tsx packages/mobile/src/components/__tests__/KeepBattleMobileGameV3.compact.test.ts
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "fix(battle): enlarge accept controls and show immediate connection"
          git push origin HEAD:reconcile/claude-main-20260825
