name: KEEP — One-shot menu size

on:
  push:
    branches:
      - reconcile/claude-main-20260825
    paths:
      - .github/workflows/one-shot-menu-size.yml

permissions:
  contents: write

concurrency:
  group: keep-one-shot-menu-size
  cancel-in-progress: true

jobs:
  apply:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Checkout source branch
        uses: actions/checkout@v4
        with:
          ref: reconcile/claude-main-20260825
          fetch-depth: 0

      - name: Sync latest branch head
        run: git pull --ff-only origin reconcile/claude-main-20260825

      - name: Apply menu size
        run: python3 scripts/one_shot_menu_size.py

      - name: Remove one-shot files
        run: rm -f scripts/one_shot_menu_size.py .github/workflows/one-shot-menu-size.yml

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm

      - name: Install
        run: npm ci --ignore-scripts

      - name: Typecheck mobile
        run: npm run type-check --workspace=packages/mobile

      - name: Typecheck music
        run: npm run type-check --workspace=packages/music

      - name: Typecheck admin
        run: npm run type-check --workspace=packages/admin

      - name: Commit standardized menu size
        run: |
          git config user.name "KEEP QA Bot"
          git config user.email "keep-qa-bot@users.noreply.github.com"
          git add -A
          if git diff --cached --quiet; then
            echo "Nothing to commit"
          else
            git commit -m "fix(mobile): standardize hamburger buttons for phone"
            git pull --rebase origin reconcile/claude-main-20260825
            git push origin HEAD:reconcile/claude-main-20260825
          fi
