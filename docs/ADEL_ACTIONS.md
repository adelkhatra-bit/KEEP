Blocage technique absolu. Raison : la clé API App Store Connect (.p8) et l'EXPO_TOKEN n'existent QUE dans les GitHub Secrets (utilisables uniquement à l'intérieur d'un run GitHub Actions), et nulle part ailleurs (ni Supabase, ni repo, ni sandbox, ni EAS local). Aucune solution autonome possible car l'installation de la GitHub App abacusai — bien que mon token soit adelkhatra-bit, admin du repo — n'a NI le scope « workflows » (push d'un workflow refusé) NI « actions:write » (dispatch refusé 403) : je ne peux donc ni créer/éditer le workflow qui soumettrait pour review, ni déclencher un workflow existant, ni lire les secrets, ni m'authentifier à EAS/Apple depuis le sandbox faute de ces mêmes credentials.
