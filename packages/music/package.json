{
  "name": "@keep/music",
  "version": "0.1.0",
  "private": true,
  "description": "KEEP — architecture musicale provider-agnostic (MusicProviderAdapter, MusicRecognitionProvider, TrackResolver, SmartPlaylistRouter). Utilisé par packages/mobile et packages/backend.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "jest",
    "type-check": "tsc --noEmit",
    "lint": "eslint . --ext .ts"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "@types/jest": "^29.5.12",
    "@types/node": "^20.10.6"
  }
}
